<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:Editor.Templates">
    <DataTemplate x:Key="RefreshBallTemplate">
        <Button Content="R"
                Command="{Binding RelativeSource={RelativeSource 
                                  AncestorType={x:Type UserControl}}, 
                                  Path=DataContext.RefreshBall}"/>
    </DataTemplate>
    <DataTemplate x:Key="IsFlaggedTemplate">
        <CheckBox IsChecked="{Binding Value}"/>
    </DataTemplate>
    <DataTemplate x:Key="IsCanceledTemplate">
        <CheckBox IsChecked="{Binding Value}" />
    </DataTemplate>
    <DataTemplate x:Key="ReadOnlyTextTemplate">
        <TextBlock Text="{Binding Value}"/>
    </DataTemplate>
    <DataTemplate x:Key="ComboBoxTemplate">
        <ComboBox ItemsSource="{Binding Items}"
                  SelectedItem="{Binding Value.Name}"
                  DisplayMemberPath="Name"/>
    </DataTemplate>
</ResourceDictionary>